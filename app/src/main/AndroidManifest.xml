<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.el.ariby">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.main.menu.club.ClubSearchActivity">
        </activity>
        <activity android:name=".ui.main.menu.club.ClubCreateActivity" />
        <activity android:name=".ui.join.NicknameActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstActivity" />
        <activity android:name=".ui.join.HeightActivity" />
        <activity android:name=".ui.join.WeightActivity" />
        <activity android:name=".ui.join.GenderActivity" />
        <activity android:name=".ui.join.AgeActivity" />
        <activity android:name=".ui.join.JoiningActivity" />
    </application>

</manifest>